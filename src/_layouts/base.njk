<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body>
    <header>
        <nav>
            <a href="{{ '/' | url }}">Home</a>
            <a href="{{ '/blog' | url }}">Blog</a>
            <a href="{{ '/about' | url }}">About</a>
            <a href="{{ '/faq' | url }}">FAQ</a>
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <p>&copy; {{ page.date.getFullYear() }} Your Name. All rights reserved.</p>
    </footer>
</body>
</html> 